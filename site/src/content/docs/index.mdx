---
title: YT Metrics CLI
description: A Python CLI for multi-channel YouTube analytics and transcript extraction.
template: splash
hero:
  tagline: Stop clicking through YouTube Studio dashboards one channel at a time. Analyze dozens of channels in a single command — engagement metrics, structured reports, and transcripts, straight from your terminal.
  image:
    file: ../../assets/logo.svg
    alt: YT Metrics CLI logo
  actions:
    - text: Get Started
      link: /yt-metrics-cli/getting-started/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/mlorentedev/yt-metrics-cli
      icon: external
      variant: minimal
---

import { CardGrid, Card, Badge } from '@astrojs/starlight/components';

## The problem

Comparing YouTube channels means opening Studio for each one, copy-pasting numbers into a spreadsheet, and writing formulas by hand. By the time you can start analyzing, hours are gone. There is no way to batch-fetch metrics, rank channels against each other, or track trends over time without building your own tooling.

## The solution

YT Metrics CLI wraps the YouTube Data API v3 in a single CLI. Point it at a YAML file of channels, run one command, and get a timestamped folder of reports: CSV for raw data, TXT for human-readable analysis, URL lists for the best and latest content. Every video gets six engagement metrics computed automatically.

## Features

<CardGrid stagger>
  <Card title="Multi-Channel Batch Analysis" icon="list-format">
    Define channels by `@handle`, legacy username, or raw ID in a simple YAML file.
    One command fetches up to 100 videos per channel with full statistics — batched in
    groups of 50 to respect API limits.
  </Card>
  <Card title="Six Engagement Metrics" icon="bars">
    Engagement rate, view rate, like rate, comment rate, subscriber-relative engagement,
    and views per minute. Every formula is transparent: `(likes + comments) / views * 100`.
    No black boxes.
  </Card>
  <Card title="Five Export Formats" icon="document">
    Every run writes a timestamped folder with CSV data, per-channel stats (top 5 videos,
    performance distribution), cross-channel trends (duration categories, viral detection),
    and URL lists for the best and latest videos. Plus a self-documenting README.
  </Card>
  <Card title="Transcript Downloader" icon="translate">
    Fetch video transcripts with multi-language support. Tries your preferred languages
    first, falls back to the first available transcript, and finally checks a local
    fixtures directory for offline use.
  </Card>
</CardGrid>

## Quick Start

```bash
# Clone and install
git clone https://github.com/mlorentedev/yt-metrics-cli.git
cd yt-metrics-cli
task install

# Configure
cp .env.example .env        # Set YOUTUBE_API_KEY
vi channels.yml             # Add channels to analyze

# Analyze channels — reports land in output/<timestamp>/
task run:channels

# Download a single video transcript
task run:video
```

## Built with

<CardGrid>
  <Card title="Python 3.12+" icon="seti:python">
    Type-hinted throughout. Modular architecture with clear separation between API, metrics, and export layers.
  </Card>
  <Card title="YouTube Data API v3" icon="external">
    Channel resolution (handle, username, ID), playlist enumeration, batch video statistics.
  </Card>
  <Card title="youtube-transcript-api" icon="translate">
    Multi-language transcript fetch with automatic fallback chain.
  </Card>
  <Card title="Task + Poetry" icon="setting">
    Reproducible builds with Poetry lockfile. Task runner for one-command install, run, and update workflows.
  </Card>
</CardGrid>
